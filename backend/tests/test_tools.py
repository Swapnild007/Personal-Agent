from pathlib import Path\n\nfrom agent.tools import CommandPolicy, ToolRegistry, Workspace\n\n\ndef test_workspace_rejects_escape(tmp_path: Path):\n    workspace = Workspace(tmp_path / "workspace")\n    try:\n        workspace.resolve("../outside")\n    except ValueError:\n        return\n    raise AssertionError("workspace escape was not rejected")\n\n\ndef test_patch_requires_unique_match(tmp_path: Path):\n    workspace = Workspace(tmp_path / "workspace")\n    tools = ToolRegistry(workspace)\n    tools.write_file("a.txt", "hello\\nhello\\n")\n    try:\n        tools.patch_file("a.txt", "hello", "bye")\n    except RuntimeError:\n        return\n    raise AssertionError("ambiguous patch was not rejected")\n\n\ndef test_command_policy_requires_approval_for_rm_and_git_mutation():\n    policy = CommandPolicy()\n    assert policy.inspect("rm -rf build") is not None\n    assert policy.inspect("git commit -m test") is not None\n    assert policy.inspect("git status") is None\n    assert policy.inspect("python -m pytest") is None\n