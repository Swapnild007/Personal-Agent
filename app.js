const modes=document.querySelectorAll(".mode");const label=document.querySelector("#mode-label");const placeholder=document.querySelector("#placeholder");const prompts={General:"Ask Personal-Agent anything...",Coding:"Describe the code you want to build or debug...",Tutor:"What would you like to learn today..."};modes.forEach(btn=>btn.addEventListener("click",()=>{modes.forEach(x=>x.classList.remove("active"));btn.classList.add("active");const mode=btn.dataset.mode;label.textContent=mode.toUpperCase()+" AGENT";placeholder.textContent=prompts[mode]}));document.querySelector("#send").addEventListener("click",()=>{const mode=document.querySelector(".mode.active").dataset.mode;document.querySelector(".core-state").textContent="LISTENING";setTimeout(()=>document.querySelector(".core-state").textContent=mode.toUpperCase(),500)});document.querySelector("#settings").addEventListener("click",()=>alert("Settings will be connected to the agent core in V2."));